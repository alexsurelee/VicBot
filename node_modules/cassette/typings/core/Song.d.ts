/// <reference types="node" />
import { Readable as ReadableStream } from 'stream';
import { IService } from '../interfaces/IService';
export default abstract class Song {
    readonly service: IService;
    loop: boolean;
    readonly abstract type: string;
    readonly abstract title: string;
    readonly abstract trackID: string | number;
    readonly abstract playlistID?: string | number;
    readonly abstract streamURL: string;
    constructor(service: IService);
    toggleLoop(): boolean;
    abstract next(): Promise<Song | null>;
    abstract stream(): Promise<ReadableStream> | ReadableStream;
}
