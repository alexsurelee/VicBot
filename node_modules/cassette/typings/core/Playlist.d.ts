import { IService } from '../interfaces/IService';
import Song from './Song';
export declare type SearchType = 'song' | 'playlist';
export default class Playlist extends Array<Song> {
    loop: boolean;
    autoplay: boolean;
    private _pos;
    readonly pos: number;
    readonly current: Song;
    reset(): void;
    hasPrev(): boolean;
    prev(): boolean;
    hasNext(): boolean;
    next(): Promise<boolean>;
    shuffle(): this;
    add(content: string, services: IService[], {position, searchType}?: {
        position?: number;
        searchType?: SearchType;
    }): Promise<Song[]>;
}
