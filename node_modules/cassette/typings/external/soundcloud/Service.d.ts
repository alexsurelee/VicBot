import { AxiosInstance } from 'axios';
import { SearchType } from '../../core/Playlist';
import SoundcloudSong from './Song';
import { IFetchable } from '../../interfaces/IFetchable';
import { IService } from '../../interfaces/IService';
import { Track } from '../../typings/Soundcloud';
export default class SoundcloudService implements IService {
    static isViewURL(test: string): boolean;
    readonly request: AxiosInstance;
    search: boolean;
    constructor(key: string);
    formatSongs(songs: Track[], playlistID?: number): SoundcloudSong[];
    fetch(fetchable: IFetchable, searchType?: SearchType): Promise<SoundcloudSong[]>;
    fetchable(content: string): IFetchable;
}
